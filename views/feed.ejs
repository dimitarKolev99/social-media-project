<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Social Media | Feed</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/feed.css">
</head>

<body>
    <% if (loggedIn) { %>
        <a href="/feed/create">Create New Post</a>
        <a href="/profile/logout">Log Out</a>

        <% if (posts !==undefined) { %>
            <div class="card-container">
                <% for (var i=0; i < posts.length; i++) { %>
                    <div class="card" style="margin-bottom: 1.2rem;">
                        <div class="flex-row">
                            <img src="<%= posts[i].authorId.imageUrl %>" width="40px" alt="mitko photo"
                                style="margin: 1rem 0.3rem 1rem 1.2rem; border-radius: 20px">
                            <a class="name" href="/profile/<%= posts[i].authorId._id %>">
                                <%= posts[i].authorId.username %>
                            </a>
                        </div>
                        <p>
                            <%= posts[i].content %>
                        </p>
                        <img src="<%= posts[i].imageUrl %>" width="100%" alt="post photo">
                    </div>

                    <% } %>
            </div>
        <% } %>

    <%} else {%>

        <h3>Please login to view this content.</h3>

        <% } %>
</body>

</html>